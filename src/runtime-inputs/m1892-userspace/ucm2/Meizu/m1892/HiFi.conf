SectionVerb {
	EnableSequence [
		cset "name='Left PCM Source' ASP"
		cset "name='Right PCM Source' ASP"
		cset "name='Left PCM Soft Ramp' 30ms"
		cset "name='Right PCM Soft Ramp' 30ms"
		cset "name='Left Digital PCM Volume' 817"
		cset "name='Right Digital PCM Volume' 817"
		cset "name='Left Analog PCM Volume' 15"
		cset "name='Right Analog PCM Volume' 17"
		cset "name='QUAT_MI2S_RX Audio Mixer MultiMedia1' 1"
		# Keep one real capture endpoint available in the default profile.
		# callaudiod requires both a usable sink and source before it can
		# switch the card into the Voice Call verb.
		cset "name='MultiMedia2 Mixer SLIMBUS_0_TX' 1"
	]

	DisableSequence [
		cset "name='QUAT_MI2S_RX Audio Mixer MultiMedia1' 0"
		cset "name='MultiMedia2 Mixer SLIMBUS_0_TX' 0"
		cset "name='Left Digital PCM Volume' 0"
		cset "name='Right Digital PCM Volume' 0"
		cset "name='Left Analog PCM Volume' 0"
		cset "name='Right Analog PCM Volume' 0"
	]

	Value {
		TQ "HiFi"
	}
}

SectionDevice."Speaker" {
	Comment "Built-in stereo speakers"

	Value {
		PlaybackPriority 300
		# VoiceMMode1 is PCM 0 in the product DT; normal MultiMedia1
		# playback remains PCM 1 and MultiMedia2 capture is PCM 2.
		PlaybackPCM "hw:${CardId},1"
		PlaybackChannels 2
	}
}

SectionDevice."Mic" {
	Comment "Built-in microphone"

	EnableSequence [
		# Flyme handset-mic and the upstream SDM845 WCD9340 examples use
		# AMIC1 -> ADC1 -> DEC6 -> SLIM TX6 for the primary microphone.
		cset "name='AIF1_CAP Mixer SLIM TX6' 1"
		cset "name='CDC_IF TX6 MUX' DEC6"
		cset "name='ADC MUX6' AMIC"
		cset "name='AMIC MUX6' ADC1"
		cset "name='IIR0 INP0 MUX' DEC6"
		cset "name='ADC1 Volume' 12"
	]

	DisableSequence [
		cset "name='ADC1 Volume' 0"
		cset "name='AMIC MUX6' ZERO"
		cset "name='CDC_IF TX6 MUX' ZERO"
		cset "name='AIF1_CAP Mixer SLIM TX6' 0"
	]

	Value {
		CapturePriority 300
		CaptureChannels 1
		CapturePCM "hw:${CardId},2"
	}
}
