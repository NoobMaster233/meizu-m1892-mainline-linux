# 已知问题

**简体中文** | [English](KNOWN_ISSUES_EN.md)

以下状态适用于当前开发者预览版：

- 自动回归证据来自一台 128 GB 设备，不代表当前全部资产都经过全量重跑；
  尚无第二台设备或 64 GB 实机验收。
- 单一运营商/SIM 上已实测短信收发、来电、去电、铃声、听筒、外放、两者切换
  和上行麦克风。该路径依赖运营商 IMS/VoLTE，不能据此推定其他运营商可用；
  通话静音尚未验收，外放模式暂时仍使用听筒麦克风路由，远场拾音和回声控制
  可能弱于 Flyme。紧急呼叫未经测试，屏下指纹也不支持。
- 基础镜像锁定密码且不携带 SSH key；快速安装要求注入机主已有公钥。没有
  图形化创建账户向导，桌面会自动登录。桌面账户属于 `docker` 组，因而具有
  等价 root 的主机控制能力；设备物理访问边界应按开发者设备对待。
- 启动到桌面通常约一分钟，固件重试时可能更久。
- UFS 完整 runtime PM level 3 会造成硬挂，因此使用安全 AH8 与显式系统挂起。
- 接入 USB 电源时关机可能重新启动 Linux；尚无仅充电关机界面。
- 充电限制在已验证的 5 V 路径，未启用私有 mCharge/24 W。
- 扬声器可用但最大响度低于 Flyme。
- Phosh 0.56 横屏快速设置面板底边关闭区域较窄。
- Stevia 键盘已验证英文输入；中文输入切换尚未成为发布门槛。
- Docker Engine、Compose 和常用网络/存储能力已通过 arm64 容器验收；这不提供
  x86 容器透明仿真，也不等同于带 KVM 的桌面虚拟机平台。
- 同一 8 GiB sparse 用于 64/128 GB；128 GB 在线扩容已验证，64 GB 只经过
  fail-closed 边界逻辑检查，没有独立实机证据。
- 内核和 EDK2 的源码与输入可审计，Actions 会执行语义/ABI 验收并记录实际
  哈希，但不同宿主仍可能生成不同的内核、模块、System.map 或 EDK2 字节。
  公开 base 尚无从空目录逐包重建的完整路径。

当前可用能力以 [公开发布状态](PUBLICATION_STATUS.md) 为准；未明确列为通过的
项目不应推定可用。
